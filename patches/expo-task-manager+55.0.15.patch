diff --git a/node_modules/expo-task-manager/android/src/main/java/expo/modules/taskManager/TaskService.java b/node_modules/expo-task-manager/android/src/main/java/expo/modules/taskManager/TaskService.java
index 44cd804..54ea94b 100644
--- a/node_modules/expo-task-manager/android/src/main/java/expo/modules/taskManager/TaskService.java
+++ b/node_modules/expo-task-manager/android/src/main/java/expo/modules/taskManager/TaskService.java
@@ -59,7 +59,7 @@ public class TaskService implements SingletonModule, TaskServiceInterface {
   private static final String SHARED_PREFERENCES_NAME = "TaskManagerModule";
   private static final int MAX_TASK_EXECUTION_TIME_MS = 15000; // 15 seconds

-  private WeakReference<Context> mContextRef;
+  private final Context mApplicationContext;
   private TaskManagerUtilsInterface mTaskManagerUtils;

   // Map with task managers of running (foregrounded) apps. { "<appScopeKey>": WeakReference(TaskManagerInterface) }
@@ -82,8 +82,11 @@ public class TaskService implements SingletonModule, TaskServiceInterface {

   public TaskService(Context context) {
     super();
-    mContextRef = new WeakReference<>(context);
-    mTasksAndEventsRepository = TasksAndEventsRepository.create(context);
+    // TaskService is kept in a static field by TaskManagerPackage, so a React or
+    // OTA reload can outlive the context that originally created it. Retain the
+    // process-scoped application context to keep background task persistence valid.
+    mApplicationContext = context.getApplicationContext();
+    mTasksAndEventsRepository = TasksAndEventsRepository.create(mApplicationContext);

     if (!mTasksAndEventsRepository.tasksExist()) {
       mTasksAndEventsRepository.createTasks();
@@ -523,8 +526,7 @@ public class TaskService implements SingletonModule, TaskServiceInterface {
   }

   private SharedPreferences getSharedPreferences() {
-    Context context = mContextRef.get();
-    return context != null ? context.getSharedPreferences(SHARED_PREFERENCES_NAME, Context.MODE_PRIVATE) : null;
+    return mApplicationContext.getSharedPreferences(SHARED_PREFERENCES_NAME, Context.MODE_PRIVATE);
   }

   private void maybeUpdateAppUrlForAppScopeKey(String appUrl, String appScopeKey) {
